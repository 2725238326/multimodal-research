# 项目规则

## 1. 事实与结论

- 本仓库是唯一项目事实来源；外部论文和代码必须经原始来源核验后才能进入正式结论。
- 单次运行、单模型结果或视觉样例不能写成稳定结论。
- 每项定量结果必须带实验 ID、数据版本、样本与独立单元数量、模型版本、指标定义、随机种子和完成状态。
- 置信区间跨零、对照失败或未达到预设门槛时，明确记录 `No-Go` 或 `Needs verification`。

## 2. Git 边界

| 内容 | 默认策略 |
| --- | --- |
| 自有脚本、测试、可移植配置 | 可提交 |
| 实验计划、决策、聚合结果摘要、文献索引 | 可提交 |
| 小型脱敏示例或 schema | 审核后可提交 |
| 原始/处理数据、权重、逐样本预测、完整日志 | 保持本地或服务器 |
| PDF、Word、QA 渲染页、缓存、压缩包 | 保持本地 |
| 第三方完整工程 | 保持本地，记录来源与 commit |
| 密钥、令牌、口令、个人凭据 | 禁止进入仓库和日志 |

`.gitignore` 只阻止新文件被加入；已经跟踪的资产需要单独的仓库清理决策。

## 3. 实验门禁

正式运行前必须具备：

1. 研究问题和可证伪假设；
2. 数据来源、许可、划分和独立统计单元；
3. 基线、干预、负对照和停止条件；
4. 模型、权重、代码版本和环境；
5. 主指标、保护指标和统计方法；
6. 实验 ID、配置和可复现命令。

先做小规模 smoke test。只有输入输出、指标和资源消耗验证通过后，才能扩大运行。

## 4. 路径与环境

- 共享配置使用仓库相对路径或环境变量。
- 服务器绝对路径放入不跟踪的本地覆盖配置。
- 日志不得记录环境变量全集、认证头或命令行密钥。
- 数据和模型目录视为不可变输入；派生文件写入独立版本目录。

## 5. 协作

- `main` 保持可复现和可交接；研究变更使用短期分支和聚焦提交。
- 开始任务先同步并检查状态，不覆盖他人工作。
- 状态、计划和证据必须能让下一位执行者不依赖聊天记录继续。
- 删除已跟踪数据、清理历史、强制推送或改写共享历史必须先获得明确同意。

## 6. 科研设计模式

- 新方向必须引用 `docs/research-design-patterns.md` 中的一个或多个模式，并写明对应反模式。
- 每个模态必须声明角色：`Measurement`、`Proposal`、`Prior`、`Router`、`Supervisor` 或 `Verifier`。
- 语义先验不得替代未观测的物理真值；物理 claim 必须有物理测量、几何约束或可证伪验证器。
- 探索性实验与确认性实验分轨。探索结果只能生成新假设，不能直接升级为稳定结论。
- 跨领域方法先填写机制迁移卡，说明不能迁移的假设、最近工作碰撞和 1–2 天 No-Go 条件。
- 一个确认性实验原则上只改变一个机制；组合方法必须通过析因设计或逐层消融归因。
- 提示、辅助模态或外部知识干预至少包含正确、错误/反向、打乱和无关四类控制。
- 同一时间最多维护一个主线确认性实验、两个邻接 gate 和一个高风险探索；其余方向保留在候选池。
